import { CleanCategoryTextPipe } from './clean-category-text.pipe';

describe('CleanCategoryTextPipe', () => {
  it('create an instance', () => {
    const pipe = new CleanCategoryTextPipe();
    expect(pipe).toBeTruthy();
  });
});
