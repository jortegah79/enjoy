import { PrepareImagePipe } from './prepare-image.pipe';

describe('PrepareImagePipe', () => {
  it('create an instance', () => {
    const pipe = new PrepareImagePipe();
    expect(pipe).toBeTruthy();
  });
});
